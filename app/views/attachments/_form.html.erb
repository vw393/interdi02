<%= simple_form_for(@attachment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :descr %>
    <%= f.association :doc %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
